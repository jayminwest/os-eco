{"id":"os-eco-ff85","title":"CLI alignment: unified UX across ecosystem","status":"closed","type":"epic","priority":1,"createdAt":"2026-02-24T03:25:08.467Z","updatedAt":"2026-02-27T16:08:27.344Z","description":"Align all four CLIs (mulch, sd, cn, overstory) on framework, output, error handling, and command patterns. Mulch is the reference implementation. Big bang for Seeds and Canopy; incremental for Overstory.","closedAt":"2026-02-27T16:08:27.344Z"}
{"id":"os-eco-c9ca","title":"Seeds: adopt Commander for CLI parsing","status":"closed","type":"feature","priority":1,"createdAt":"2026-02-24T03:25:15.205Z","updatedAt":"2026-02-27T15:59:16.395Z","description":"Seeds currently uses manual process.argv parsing with a switch statement in src/index.ts. Migrate to Commander (like Mulch) to get: structured arg/option definitions, auto-generated help with argument types and choices, .addHelpText() for detailed per-command docs, built-in version display. This is a big-bang rewrite of the CLI layer — command logic stays, only the parsing/routing changes. Reference: mulch/src/cli.ts for the pattern. Each command in src/commands/ should export a register function that takes a Commander program instance.","blocks":["os-eco-4b0e"],"closedAt":"2026-02-26T22:46:09.625Z"}
{"id":"os-eco-e3ba","title":"Overstory multi-repo mode: spawn agents into sub-repos","status":"closed","type":"feature","priority":2,"createdAt":"2026-02-24T03:25:17.685Z","updatedAt":"2026-03-03T20:28:24.153Z","description":"Add --repo flag to overstory sling/merge so the root overstory instance can spawn agents into sub-repo worktrees (seeds, mulch, canopy, overstory) and merge them back. Config adds a repos map in .overstory/config.yaml. Mail/sessions/metrics stay in root .overstory/ for unified coordination. Main changes: sling.ts (worktree creation targeting sub-repo git dir), merge.ts (repo-aware merge), config.ts (repos map).","closedAt":"2026-03-03T20:28:24.153Z"}
{"id":"os-eco-4b0e","title":"Seeds: adopt chalk for output formatting","status":"closed","type":"feature","priority":1,"createdAt":"2026-02-24T03:25:20.225Z","updatedAt":"2026-02-26T22:46:10.710Z","description":"Seeds uses raw ANSI escape codes in src/output.ts with manual TTY/NO_COLOR detection. Migrate to chalk (like Mulch) for: consistent color API across the ecosystem, automatic terminal capability detection, cleaner output code. Current Seeds patterns to migrate: c.green/c.red/c.bold/c.dim helper functions → chalk.green/chalk.red/chalk.bold/chalk.dim. Status icons (●/◐/○) and printIssueOneLine/printIssueFull formatting should be preserved but use chalk underneath. The outputJson/printSuccess/printError helpers should match Mulch's patterns (✔ green for success, Error: red for errors).","blockedBy":["os-eco-c9ca"],"closedAt":"2026-02-26T22:46:10.710Z"}
{"id":"os-eco-3043","title":"Seeds: align error handling with ecosystem pattern","status":"closed","type":"feature","priority":2,"createdAt":"2026-02-24T03:25:24.316Z","updatedAt":"2026-02-27T16:00:42.132Z","description":"Seeds uses process.exit(1) in its main catch block. Migrate to process.exitCode = 1 (Mulch pattern) for better testability — allows event loop to drain and vitest/bun test to capture output cleanly. Also standardize JSON error shape: currently { success: false, command, error } which already matches Mulch. Ensure all commands use consistent error surfacing: console.error() with chalk.red in human mode, structured JSON in --json mode.","closedAt":"2026-02-27T16:00:42.132Z"}
{"id":"os-eco-d4e0","title":"Canopy: adopt Commander for CLI parsing","status":"closed","type":"feature","priority":1,"createdAt":"2026-02-24T03:25:30.825Z","updatedAt":"2026-02-27T15:59:16.435Z","description":"Canopy uses manual process.argv parsing with a switch statement in src/index.ts, plus per-command flag parsing via local helpers. Migrate to Commander (like Mulch) to get: structured arg/option definitions, auto-generated help, choice validation, .addHelpText() for detailed docs. Big-bang rewrite of CLI layer. Each command in src/commands/ should export a register function. Reference: mulch/src/cli.ts. Notable: Canopy has subcommands (schema create/show/list, schema rule add) — Commander supports this via .command().command() nesting.","blocks":["os-eco-4ebc"],"closedAt":"2026-02-26T22:46:10.152Z"}
{"id":"os-eco-4ebc","title":"Canopy: adopt chalk for output formatting","status":"closed","type":"feature","priority":1,"createdAt":"2026-02-24T03:25:34.873Z","updatedAt":"2026-02-26T22:46:11.413Z","description":"Canopy uses raw ANSI codes in src/output.ts with manual NO_COLOR/TTY detection. Migrate to chalk (like Mulch) for consistent ecosystem UX. Current Canopy patterns to migrate: c.bold/c.dim/c.green/c.red/c.yellow/c.cyan/c.blue helpers → chalk equivalents. Preserve the humanOut/jsonOut/errorOut helper pattern but use chalk underneath. Status indicators (✓/✗/→) should match Mulch's style (✔ for success, Error: prefix for failures).","blockedBy":["os-eco-d4e0"],"closedAt":"2026-02-26T22:46:11.413Z"}
{"id":"os-eco-7dc3","title":"Canopy: align error handling with ecosystem pattern","status":"closed","type":"feature","priority":2,"createdAt":"2026-02-24T03:25:36.130Z","updatedAt":"2026-02-27T16:01:22.951Z","description":"Canopy has a custom ExitError class and uses both process.exit(1) and throw ExitError(1). Migrate to process.exitCode = 1 pattern (Mulch standard) for testability. The ExitError class can remain for internal flow control but the top-level handler should set process.exitCode rather than calling process.exit(). JSON error shape should be standardized: { success: false, command, error }.","closedAt":"2026-02-27T16:01:22.951Z"}
{"id":"os-eco-c130","title":"Overstory: incrementally adopt Commander for CLI parsing","status":"closed","type":"feature","priority":2,"createdAt":"2026-02-24T03:25:42.483Z","updatedAt":"2026-02-27T16:08:27.344Z","description":"Overstory uses manual argv parsing with local getFlag()/hasFlag()/parseArgs() helpers across 30+ commands. Incremental migration plan: (1) Add Commander as dependency, (2) Migrate simple commands first (init, status, clean, doctor), (3) Then migrate complex commands with subcommands (agents, coordinator, supervisor, mail, run, group, spec, hooks, worktree), (4) Finally migrate observability commands (dashboard, inspect, trace, errors, replay, feed, logs). Each migration should be a standalone PR. Reference: mulch/src/cli.ts for the command registration pattern.","blocks":["os-eco-c2ef"],"closedAt":"2026-02-27T16:08:27.344Z"}
{"id":"os-eco-62f5","title":"Overstory: adopt chalk for output formatting","status":"closed","type":"feature","priority":2,"createdAt":"2026-02-24T03:25:46.178Z","updatedAt":"2026-02-27T16:08:27.344Z","description":"Overstory has its own ANSI color system in src/logging/color.ts with manual escape codes and NO_COLOR/FORCE_COLOR/TERM=dumb detection. Incrementally migrate to chalk: (1) Replace src/logging/color.ts internals with chalk while keeping the same API surface, (2) Then gradually migrate direct color usage in commands to chalk calls. This can be done in parallel with Commander migration. Overstory's color system is more sophisticated (has FORCE_COLOR) — chalk handles all of this natively.","closedAt":"2026-02-27T16:08:27.344Z"}
{"id":"os-eco-8748","title":"Overstory: align error handling with ecosystem pattern","status":"closed","type":"feature","priority":3,"createdAt":"2026-02-24T03:25:50.221Z","updatedAt":"2026-02-27T16:08:27.344Z","description":"Overstory has a rich error hierarchy (OverstoryError → ConfigError, AgentError, HierarchyError, MailError, ValidationError, MergeError, WorktreeError) with error codes. This is the most mature error system in the ecosystem. Keep the error class hierarchy but align exit behavior: use process.exitCode = 1 instead of process.exit(1) where possible. JSON error shape should include the error code field: { success: false, command, error, code }. The error code pattern could be adopted by other tools eventually.","closedAt":"2026-02-27T16:08:27.344Z"}
{"id":"os-eco-1537","title":"Standardize shared command patterns across ecosystem","status":"closed","type":"feature","priority":1,"createdAt":"2026-02-24T03:25:59.712Z","updatedAt":"2026-02-27T16:08:27.344Z","description":"All four tools have overlapping commands (sync, prime, doctor, onboard) with inconsistent flags and behavior. Align them:\n\n**sync**: All tools have sync. Standardize flags: --status (dry-run check) across all. Currently consistent.\n\n**prime**: All tools have prime. Mulch has --verbose/--full/--format/--files/--context. Seeds has --compact. Canopy has --compact/--export. Overstory has its own patterns. Align: all should support --compact and --format at minimum.\n\n**doctor**: Mulch has --fix. Seeds has --fix. Overstory has --category/--verbose. Canopy lacks doctor. Align: all should support --fix and --verbose at minimum.\n\n**onboard**: Mulch and Seeds and Canopy all have onboard. Standardize the --check/--stdout flags across all.\n\n**help text**: All --help output should follow Commander's format: Usage line, Description, Arguments, Options with types/defaults, Examples via addHelpText.\n\n**--json**: All tools support --json. Ensure consistent behavior: always outputs to stdout, errors still to stderr, response shape { success, command, data/error }.","closedAt":"2026-02-27T16:08:27.344Z"}
{"id":"os-eco-c2ef","title":"Add shell completion support to Seeds and Canopy","status":"closed","type":"feature","priority":3,"createdAt":"2026-02-24T03:26:03.741Z","updatedAt":"2026-02-27T16:08:27.344Z","description":"Overstory has shell completions (--completions bash/zsh/fish) via src/commands/completions.ts. Mulch has no completions (Commander can generate basic ones). Seeds and Canopy have none. Once Commander is adopted across all tools, add shell completion generation. Commander has built-in completion support or can be extended. Overstory's completion system (CommandDef/FlagDef/SubcommandDef types) is a good reference but should be replaced with Commander's native approach once migrated.","blockedBy":["os-eco-c130"],"closedAt":"2026-02-27T16:08:27.344Z"}
{"id":"os-eco-5890","title":"Standardize version management across ecosystem","status":"closed","type":"feature","priority":3,"createdAt":"2026-02-24T03:26:07.913Z","updatedAt":"2026-02-27T16:08:27.344Z","description":"Version handling differs across tools:\n- Mulch (0.5.0): VERSION in package.json, program.version() in cli.ts, has version check utility (npm registry)\n- Seeds (0.2.0): VERSION const in src/index.ts + package.json, manual --version flag\n- Canopy (0.1.2): VERSION const in src/index.ts + package.json, manual --version flag  \n- Overstory (0.6.0): VERSION const in src/index.ts + package.json, manual --version flag\n\nAll have scripts/version-bump.ts. Once Commander is adopted, use program.version() consistently. Consider whether each tool should have an update-check like Mulch's getLatestVersion().","closedAt":"2026-02-27T16:08:27.344Z"}
{"id":"os-eco-2b32","title":"Stop hook error: mulch not found in agent PATH","status":"closed","type":"bug","priority":2,"createdAt":"2026-02-24T14:06:44.987Z","updatedAt":"2026-02-24T14:42:37.227Z","closedAt":"2026-02-24T14:42:37.227Z","closeReason":"Fixed manually. Follow-up doctor checks tracked in overstory seeds-93a8."}
{"id":"os-eco-ca1c","title":"Add --root flag to sd create for writing to project root .seeds/","status":"closed","type":"feature","priority":3,"createdAt":"2026-02-24T16:16:43.298Z","updatedAt":"2026-02-24T16:17:18.910Z","description":"When agents run in git worktrees, sd create writes to the worktree .seeds/ directory. If that branch is never merged (e.g. lead agents), those issues are lost. Add a --root flag that writes directly to the project root .seeds/ instead of the worktree copy, so issues survive worktree cleanup regardless of merge status.","closedAt":"2026-02-24T16:17:18.910Z","closeReason":"Refiled in seeds sub-repo"}
{"id":"os-eco-6199","title":"Sapling × Overstory: Phase 1 runtime adapter","status":"closed","type":"epic","priority":1,"createdAt":"2026-03-03T20:53:52.987Z","updatedAt":"2026-03-03T22:48:18.009Z","description":"Cross-repo tracking issue. Implement sapling as an overstory runtime adapter with direct subprocess management (no tmux). See docs/sapling-overstory-integration.md Phase 1. Work split between overstory/ and sapling/.","blocks":["os-eco-b24c"],"closedAt":"2026-03-03T22:48:18.009Z","closeReason":"Phase 1 runtime adapter complete across both repos. 11 issues closed."}
{"id":"os-eco-b24c","title":"Sapling × Overstory: Phase 2 native hook system","status":"open","type":"epic","priority":2,"createdAt":"2026-03-03T20:53:54.297Z","updatedAt":"2026-03-03T20:54:37.731Z","description":"Cross-repo tracking issue. Sapling-native hooks/guards configured by overstory, enforced inside sapling tool registry. Per-tool-call NDJSON events. See docs/sapling-overstory-integration.md Phase 2.","blockedBy":["os-eco-6199"],"blocks":["os-eco-c272"]}
{"id":"os-eco-c272","title":"Sapling × Overstory: Phase 3 native ecosystem integration","status":"open","type":"epic","priority":3,"createdAt":"2026-03-03T20:53:55.550Z","updatedAt":"2026-03-03T20:54:37.731Z","description":"Cross-repo tracking issue. Direct bun:sqlite access to mail.db/metrics.db, direct mulch/seeds calls from sapling agents. Eliminates subprocess overhead. See docs/sapling-overstory-integration.md Phase 3.","blockedBy":["os-eco-b24c"]}
{"id":"os-eco-d277","title":"Test sapling runtime with MiniMax backend","status":"closed","type":"task","priority":3,"createdAt":"2026-03-04T05:30:13.033Z","updatedAt":"2026-03-04T13:41:20.099Z","closedAt":"2026-03-04T13:41:20.099Z"}
{"id":"os-eco-bc05","title":"Diagnose and fix sapling-overstory integration for MiniMax backend","status":"closed","type":"task","priority":1,"createdAt":"2026-03-04T05:34:02.926Z","updatedAt":"2026-03-04T23:39:57.114Z","description":"## Context\n\nAttempted to run overstory with --runtime sapling using MiniMax as the LLM backend. Multiple issues surfaced that need systematic resolution.\n\n## Known Issues\n\n1. CLAUDECODE env leak: spawning sapling from Claude Code leaks CLAUDECODE=1, auto-selecting sdk backend without an API key\n2. Missing prompt argument: buildDirectSpawn() was not passing a user prompt, causing sapling to hang on stdin\n3. Gateway provider translation: overstory sets ANTHROPIC_AUTH_TOKEN (for CC runtime) but sapling SDK needs ANTHROPIC_API_KEY\n4. Model alias resolution: overstory resolves gateway models to alias \"sonnet\" but sapling needs the actual model name\n5. End-to-end flow untested\n\n## IMPORTANT: Talk to the developer first\n\nThe agent working on this MUST have a conversation with the human developer before implementing anything. Ask about:\n- What they expect sapling <> overstory integration to look like\n- How MiniMax and other non-Anthropic providers should be configured\n- Whether the gateway provider pattern is right for sapling or if sapling should handle provider config independently\n- What sapling/.env is intended to do vs overstory config.yaml providers\n\nPartial fixes exist in overstory/src/runtimes/sapling.ts (uncommitted) but were written without full understanding of the intended architecture. Review before committing.\n\n## Files with uncommitted changes\n\n- overstory/src/runtimes/sapling.ts (buildEnv and buildDirectSpawn changes)\n- overstory/.overstory/config.yaml (added minimax provider config)","closedAt":"2026-03-04T23:39:57.114Z","closeReason":"Investigation complete. Filed overstory-7110 (buildDirectSpawn forces --model) and overstory-b87b (ANTHROPIC_API_KEY leak from parent env). CLAUDECODE leak already fixed."}
{"id":"os-eco-2b46","title":"Sapling goes zombie immediately when spawned by overstory (headless)","status":"closed","type":"bug","priority":2,"createdAt":"2026-03-04T13:53:33.917Z","updatedAt":"2026-03-04T18:37:06.829Z","blocks":["os-eco-a3f8"],"closedAt":"2026-03-04T18:37:06.829Z"}
{"id":"os-eco-a3f8","title":"E2E verification: sapling-overstory integration with MiniMax","status":"closed","type":"task","priority":2,"createdAt":"2026-03-04T13:53:37.065Z","updatedAt":"2026-03-05T15:40:52.563Z","blockedBy":["os-eco-2b46","os-eco-f064"],"closedAt":"2026-03-05T15:40:52.563Z"}
{"id":"os-eco-1eaa","title":"Simplify overstory sapling runtime to passthrough buildEnv after sapling env var compat","status":"closed","type":"task","priority":2,"createdAt":"2026-03-04T14:00:36.605Z","updatedAt":"2026-03-05T14:25:35.871Z","closedAt":"2026-03-05T14:25:35.871Z"}
{"id":"os-eco-afec","title":"Smoke test: sapling scout reads README","status":"closed","type":"task","priority":3,"createdAt":"2026-03-04T15:28:39.816Z","updatedAt":"2026-03-04T15:32:04.075Z","closedAt":"2026-03-04T15:32:04.075Z"}
{"id":"os-eco-be8f","title":"ov sling --runtime sapling: deployConfig called with undefined overlay skips guards.json","status":"in_progress","type":"bug","priority":2,"createdAt":"2026-03-04T15:31:35.231Z","updatedAt":"2026-03-04T23:06:19.853Z"}
{"id":"os-eco-807a","title":"Smoke test: sapling scout reads README via overstory","status":"closed","type":"task","priority":3,"createdAt":"2026-03-04T15:51:05.744Z","updatedAt":"2026-03-04T15:55:20.337Z","closedAt":"2026-03-04T15:55:20.337Z"}
{"id":"os-eco-49c7","title":"Test: sapling runtime scout exploration of project structure","status":"closed","type":"task","priority":3,"createdAt":"2026-03-04T18:31:17.501Z","updatedAt":"2026-03-04T18:34:16.261Z","closedAt":"2026-03-04T18:34:16.261Z","closeReason":"Explored worktree structure: found standard os-eco files (README.md, CLAUDE.md, SAPLING.md), branding/, .overstory/, .claude/commands/, and tool directories (.seeds/, .mulch/, .canopy/). Test scout exploration verified successfully."}
{"id":"os-eco-f064","title":"buildDirectSpawn passes hardcoded fallback instead of task description","status":"closed","type":"bug","priority":1,"createdAt":"2026-03-04T23:24:43.670Z","updatedAt":"2026-03-04T23:26:14.783Z","description":"buildDirectSpawn() in overstory/src/runtimes/sapling.ts always passes 'Read SAPLING.md for your task assignment and begin immediately.' as the prompt argument instead of the actual task/spec content. In our test run, this caused the agent to burn 42 turns and 225k input tokens wandering aimlessly. buildDirectSpawn() should compose the task description from the overlay/spec and pass it as the positional prompt argument to sp run.","blocks":["os-eco-a3f8"],"closedAt":"2026-03-04T23:26:14.783Z","closeReason":"moved to overstory-0f5f"}
{"id":"os-eco-a4e2","title":"Session state never transitions from booting for headless agents","status":"closed","type":"bug","priority":2,"createdAt":"2026-03-04T23:24:45.434Z","updatedAt":"2026-03-04T23:26:14.822Z","description":"sling.ts creates sessions with state=booting but no mechanism transitions headless agents to working. For tmux runtimes this happens via pane content detection. For sapling, the first turn_start or tool_start NDJSON event should trigger the transition. Currently ov status always shows sapling agents as booting.","closedAt":"2026-03-04T23:26:14.822Z","closeReason":"moved to overstory-7ece"}
{"id":"os-eco-faea","title":"Add centralized error classes to mulch, seeds, canopy, and greenhouse","status":"closed","type":"task","priority":3,"createdAt":"2026-03-06T00:36:09.110Z","updatedAt":"2026-05-17T21:34:18.236Z","description":"Overstory and sapling have centralized src/errors.ts files with a base error class (OverstoryError, SaplingError) extending Error with a code: string field for machine-readable handling. The other four repos (mulch, seeds, canopy, greenhouse) use generic Error handling without structured error hierarchies.\n\nAdd a centralized errors.ts to each repo following the established pattern:\n- Base class: <Tool>Error extends Error with code: string field\n- Subclasses for domain-specific errors (e.g. ConfigError, StoreError, etc.)\n- Each error class sets a name property\n\nRepos: mulch, seeds, canopy, greenhouse\nReference: overstory/src/errors.ts, sapling/src/errors.ts","closedAt":"2026-05-17T21:34:18.236Z","closeReason":"Obsolete: greenhouse archived 2026-05 (superseded by warren). The mulch/seeds/canopy portion of this work remains potentially useful — re-file as a fresh issue if still desired."}
{"id":"os-eco-9f78","title":"Overstory: track per-coordinator sessions, not just one global session","status":"closed","type":"task","priority":1,"createdAt":"2026-03-06T14:03:47.522Z","updatedAt":"2026-03-06T14:04:10.056Z","closedAt":"2026-03-06T14:04:10.056Z"}
{"id":"os-eco-6896","title":"V1 branding audit: verify all 6 tools match visual spec","status":"closed","type":"task","priority":2,"createdAt":"2026-03-07T15:15:58.907Z","updatedAt":"2026-05-17T21:34:20.890Z","description":"Cross-cutting verification. All 6 tools must match branding/visual-spec.md before V1:\n\n- Forest palette brand colors (each tool has a unique RGB)\n- Help Screen Style A (branded header, green commands, dim args, aligned descriptions)\n- Status Icon Set D (- > x ! with correct colors)\n- Message format standards (checkmark/cross/exclamation with correct colors)\n\n5 of 6 tools are compliant. Greenhouse gaps tracked in greenhouse-1789.\n\nVerification: run each tool's --help and compare against visual-spec.md template. Check status displays in list/status commands for Set D icons.","closedAt":"2026-05-17T21:34:20.890Z","closeReason":"Obsolete: greenhouse archived 2026-05. V1 branding audit was framed around the 'original six' (with greenhouse as the laggard). Active toolset is now 5 audited + 2 pending (burrow, warren) — tracked in branding/checklists.md and branding/README.md; no separate audit issue needed."}
{"id":"os-eco-639a","title":"V1 integration contract validation: verify --json interfaces between tools","status":"closed","type":"task","priority":2,"createdAt":"2026-03-07T15:15:58.944Z","updatedAt":"2026-05-17T21:34:22.973Z","description":"Cross-cutting verification. The ecosystem depends on stable --json contracts between tools:\n\n- Greenhouse -> Overstory: ov coordinator start/send/status --json\n- Greenhouse -> Seeds: sd create/show/close --json\n- Overstory -> Seeds: sd ready/update/close --json\n- Overstory -> Mulch: ml prime/record --json\n- Overstory -> Canopy: agent definition .md files emitted by cn emit\n\nVerify each contract by running the --json command and confirming the response shape matches what the consuming tool expects. Document any mismatches. This is a read-only audit — no code changes unless mismatches are found.","closedAt":"2026-05-17T21:34:22.973Z","closeReason":"Obsolete: greenhouse archived 2026-05. The 'Greenhouse -> Overstory' and 'Greenhouse -> Seeds' --json contracts are moot. Remaining intra-ecosystem --json contracts (overstory->seeds/mulch, warren->seeds/mulch) can be tracked individually if drift is observed."}
{"id":"os-eco-60e6","title":"V1 acceptance test: label issue -> autonomous PR on overstory repo","status":"closed","type":"task","priority":1,"createdAt":"2026-03-07T15:15:58.979Z","updatedAt":"2026-05-17T21:34:25.283Z","description":"The V1 litmus test for the entire ecosystem. Add an agents-ready label to an issue on the overstory repo. Walk away. Come back to a PR opened autonomously.\n\nSuccess criteria:\n- Greenhouse detects the labeled issue\n- Issue is ingested into seeds\n- Overstory coordinator decomposes and dispatches work\n- Coordinators/leads run as Claude Code sessions\n- Leaf agents (builders, scouts, reviewers) run on sapling runtime\n- Work is merged into greenhouse merge branch\n- grhs ship runs pre-flight checks and opens PR\n- Original issue gets a comment with PR link\n- Local state is clean after run: no orphaned worktrees, no zombie tmux sessions, no stale locks, no uncommitted files\n\nDepends on all per-tool V1 issues being resolved first. This is the final gate before declaring V1 done.","closedAt":"2026-05-17T21:34:25.283Z","closeReason":"Obsolete as written: greenhouse archived 2026-05. The V1 litmus test (label issue -> autonomous PR, walk away, come back to a PR) is still the right gate, but warren now plays greenhouse's role. Re-file as a warren-specific acceptance test if/when warren is ready to demonstrate the full loop end-to-end."}
{"id":"os-eco-61da","title":"Migrate overstory consumers to sapling's push-only orchestrator surface","status":"open","type":"task","priority":2,"createdAt":"2026-05-07T20:01:32.568Z","updatedAt":"2026-05-07T20:01:32.568Z","description":"Sapling no longer calls 'ov mail check' between turns or 'ov mail send task_done' on exit (sapling-3674). Overstory's SaplingRuntime must migrate to: (1) push steers/followUps via JSON-RPC stdin instead of mail polling, (2) detect task completion from the NDJSON 'result' event or the metrics-file '_exit' block instead of mail. See sapling/docs/orchestrator-migration.md for the contract and a minimal stub. OVERSTORY_* env vars are also removed — orchestrator must set SAPLING_* equivalents (or pass --agent-name / --task-id / --metrics-path)."}
{"id":"os-eco-ce89","title":"Add pi (@earendil-works/pi-coding-agent) as a third first-class runtime across burrow + warren","status":"open","type":"epic","priority":2,"createdAt":"2026-05-12T01:39:48.224Z","updatedAt":"2026-05-12T01:39:48.224Z","description":"Tracker for adding the pi coding agent (installed locally as `pi`; docs in /opt/homebrew/lib/node_modules/@earendil-works/pi-coding-agent/docs/) as a third first-class runtime alongside claude-code and sapling.\n\nWhy pi: documented JSONL RPC mode with rich event vocabulary (compaction, retry, extension UI, mid-run steering); first-class extensions/skills/prompts filesystem surface (`.pi/` with project-local overrides); 30+ model providers with native per-session cost tracking. The 'get more out of raw API spend' angle.\n\nInitial research lives in chat transcript on 2026-05-11 (Warren session) — covers pi capability map, burrow runtime-dispatch internals, pi RPC + SDK protocol detail, and a phased build proposal. Child seeds filed separately for burrow + warren.\n\nLoad-bearing finding: burrow's AgentRegistry (burrow/src/runtime/registry.ts) is already generic over agent name — the dispatch path needs no per-agent branching, just a new piRuntime entry + AgentRuntime impl. The interesting work is (a) mapping canopy agent definitions onto pi's .pi/ customization layout, (b) introducing multi-provider as a first-class warren concept, (c) widening burrow's event-kind union for compaction/retry surfaces.\n\n**This seed should be converted into a detailed sd plan once internal/external research is completed** (try out pi RPC against burrow's dispatcher manually, prototype one canopy→.pi/ section, sanity-check that pi's pre-1.0 RPC schema is stable enough to pin).","labels":["pi","runtime","burrow","warren","ecosystem"]}
{"id":"os-eco-49a0","title":"Branding pass: align sub-repo READMEs with warren-flagship root framing","status":"open","type":"task","priority":3,"createdAt":"2026-05-18T04:02:05.265Z","updatedAt":"2026-05-18T04:02:05.265Z","description":"Root README + CLAUDE.md + branding/README.md are being updated to make warren the flagship and add plot as a first-class tool in a new 'substrate' tier (alongside burrow). Sub-repo READMEs still reflect the old peer-tools framing.\n\nSub-repos to pass through:\n- warren/README.md — should read as the headline project; quickstart should be polished\n- overstory/README.md — currently presents itself as flagship-equivalent; quietly de-emphasize (no 'maintenance' label, just align tone with 'alternative orchestrator')\n- plot/README.md — 'Where Plot fits in os-eco' table is fine but should mention substrate tier and warren-first context\n- mulch/README.md — add traction signal ('in production across multiple engineering teams') without naming companies\n- burrow/README.md — note its substrate role under warren alongside plot\n- seeds/README.md, canopy/README.md, sapling/README.md — light check for consistency with root narrative\n\nDriver: user said warren has become first-class, mulch has enterprise adoption, plot is now standalone-but-substrate, overstory has fallen by the wayside. Root will be updated in this session; sub-repos deferred."}
{"id":"os-eco-f066","title":"L5 uplift: os-eco root — governance & discoverability","status":"open","type":"feature","priority":1,"createdAt":"2026-05-28T03:17:45.952Z","updatedAt":"2026-05-28T17:06:14.801Z","description":"Final milestone of the L5 uplift mission: close the four governance and discoverability gaps for the os-eco META repo (not any sub-repo). Adds AGENTS.md, .github/ scaffolding (dependabot, PR template, issue templates, labels + sync workflow), a comprehensive root .gitignore, and a section pointing at templates/l5-toolkit/ from the top-level docs.\n\nScope is governance-only because os-eco root has no package.json, no source code, no test suite, and no lint pipeline — the vast majority of rubric criteria are N/A here. The auditor will skip application-scope criteria; only repo-scope governance (AGENTS.md, .gitignore, .github/ files, toolkit discoverability) is in play.\n\nCloses VAL-OS-ECO-001 through VAL-OS-ECO-007 (plus the FINAL seal), feeding into the cross-area assertions VAL-CROSS-001..006. Reference: /Users/jayminwest/Projects/os-eco/docs/l5-uplift/validation-contract.md and docs/l5-uplift/library/criteria-mapping.md. Toolkit artifacts to copy from live under templates/l5-toolkit/github/ in this same repo.","plan_id":"pl-ef44","blockedBy":["os-eco-3ce3"]}
{"id":"os-eco-352d","title":"Add os-eco governance files (AGENTS.md, .github scaffolding)","status":"closed","type":"task","priority":2,"plan_step_index":0,"description":"# Goal\n\nAdd the governance scaffolding the os-eco META repo is missing so the L5\nauditor sees AGENTS.md, dependabot, a PR template, and the three issue\ntemplates at the repo root. **Closes VAL-OS-ECO-001, VAL-OS-ECO-002,\nVAL-OS-ECO-003, VAL-OS-ECO-004.**\n\nThis is the os-eco META repo (no package.json, no source, no tests). All\nfile paths are RELATIVE to the os-eco root. **Do not touch any sub-repo\ndirectory** — sub-repos (`warren/`, `burrow/`, `plot/`, `mulch/`,\n`seeds/`, `canopy/`, `sapling/`, `overstory/`) are independent gits and\nare gitignored from the root.\n\n# Working directory\n\n`/Users/jayminwest/Projects/os-eco/`\n\nRun all commands from there. `git status` must be clean before you start\n(you should see only this seed's tracked changes after edits).\n\n# Reference docs (live in this repo; safe to read mid-task)\n\n- `docs/l5-uplift/library/porting-playbook.md` — overall procedure.\n- `docs/l5-uplift/library/criteria-mapping.md` — per-criterion expected\n  artifact (the rows for `agents_md`, `min_release_age`,\n  `pr_templates`, `issue_templates`, `dependency_update_automation`).\n- `docs/l5-uplift/validation-contract.md` — VAL-OS-ECO-001..004\n  assertions: exact filenames and content gates.\n- `templates/l5-toolkit/github/` — source-of-truth for the artifacts.\n\n# Files to add\n\nCopy / adapt from the toolkit (`templates/l5-toolkit/github/`) into the\nos-eco root:\n\n1. **`AGENTS.md` (new, at root)** — ecosystem-wide guidance. Must be\n   >2 KB and contain an \"Ecosystem\" (or equivalent) section. Cover:\n   - One-paragraph description of os-eco as a meta-repo.\n   - Sub-repo map (the eight tools: warren, burrow, plot, mulch,\n     seeds, canopy, sapling, overstory) with their CLIs, npm names,\n     and one-line purpose. Mirror `CLAUDE.md`'s tables.\n   - Root-level commands: `scripts/run-plan.sh <plan-id>`,\n     `ml prime`, `sd prime`, `cn prime`.\n   - Cross-cutting `.seeds/`, `.mulch/`, `.canopy/`, `.overstory/`\n     roles (root-level vs sub-repo).\n   - Greenhouse retirement note (archived 2026-05; replaced by warren).\n   - Pointer to per-tool CLAUDE.md / AGENTS.md in each sub-repo.\n   - Pointer to `templates/l5-toolkit/README.md` for the L5 toolkit.\n\n2. **`.github/dependabot.yml` (new)** — Adapt\n   `templates/l5-toolkit/github/dependabot.yml`. The toolkit version\n   ships both an `npm` ecosystem and a `github-actions` ecosystem. The\n   os-eco ROOT has **no package.json**, so:\n   - DROP the `npm` ecosystem block entirely.\n   - KEEP the `github-actions` ecosystem block with the weekly schedule\n     and the `cooldown` block (default 5d; matches the toolkit).\n   - Result: a single `github-actions` updates entry.\n\n3. **`.github/pull_request_template.md` (new)** — Copy verbatim from\n   `templates/l5-toolkit/github/pull_request_template.md`. The toolkit\n   version references `bun run lint`/`typecheck`/`test`/`check:all`,\n   which are not applicable to the meta-repo. **Adapt** the \"Test plan\"\n   checkbox list so it makes sense at the meta-repo level — e.g.:\n   - [ ] `git status` clean\n   - [ ] Affected sub-repo (if any) listed in Summary\n   - [ ] Manual verification (if applicable)\n\n   Keep the Summary / Changes / Test plan section headings; the\n   validation-contract evidence sentence asserts those three are present.\n\n4. **`.github/ISSUE_TEMPLATE/bug_report.yml` (new)** — Copy from\n   `templates/l5-toolkit/github/ISSUE_TEMPLATE/bug_report.yml`. Adapt:\n   - The toolkit asks for \"Project version\" via `<cli> --version`. The\n     meta-repo has no CLI; either rename the field to \"Affected\n     sub-repo / version\" with a placeholder like\n     `warren@0.5.0, burrow@…`, OR drop the version field and keep the\n     OS + Bun fields. Pick one and keep the file syntactically valid\n     YAML.\n   - Keep the `labels: [\"kind/bug\", \"status/needs-triage\"]` declaration\n     intact so it composes cleanly with the labels.yml that the next\n     child adds.\n\n5. **`.github/ISSUE_TEMPLATE/feature_request.yml` (new)** — Copy\n   verbatim from\n   `templates/l5-toolkit/github/ISSUE_TEMPLATE/feature_request.yml`.\n   Keep `labels: [\"kind/feature\", \"status/needs-triage\"]`.\n\n6. **`.github/ISSUE_TEMPLATE/config.yml` (new)** — Copy verbatim from\n   `templates/l5-toolkit/github/ISSUE_TEMPLATE/config.yml`. Sets\n   `blank_issues_enabled: false` and points the discussions link at\n   `https://github.com/jayminwest/os-eco/discussions`. (That URL is\n   correct; this is the os-eco meta-repo.)\n\n# Verification\n\nThere is no test suite at the os-eco root, so verification is minimal:\n\n```bash\ncd /Users/jayminwest/Projects/os-eco/\nls -la AGENTS.md .github/dependabot.yml .github/pull_request_template.md \\\n       .github/ISSUE_TEMPLATE/bug_report.yml \\\n       .github/ISSUE_TEMPLATE/feature_request.yml \\\n       .github/ISSUE_TEMPLATE/config.yml\nwc -c AGENTS.md          # expect > 2048 bytes\ngrep -i \"ecosystem\" AGENTS.md  # at least one match\n# YAML validity sanity check (the toolkit copies are already valid):\npython3 -c \"import yaml; yaml.safe_load(open('.github/dependabot.yml'))\"\npython3 -c \"import yaml; yaml.safe_load(open('.github/ISSUE_TEMPLATE/bug_report.yml'))\"\npython3 -c \"import yaml; yaml.safe_load(open('.github/ISSUE_TEMPLATE/feature_request.yml'))\"\npython3 -c \"import yaml; yaml.safe_load(open('.github/ISSUE_TEMPLATE/config.yml'))\"\ngit status\n```\n\n`git status` should show all six new files as untracked / added on the\nos-eco main branch.\n\n# Acceptance criteria closed\n\n- **VAL-OS-ECO-001** — AGENTS.md at root, >100 chars (target >2 KB),\n  with an Ecosystem section.\n- **VAL-OS-ECO-002** — `.github/dependabot.yml` exists and declares a\n  github-actions update schedule.\n- **VAL-OS-ECO-003** — `.github/pull_request_template.md` exists with\n  Summary / Changes / Test plan sections.\n- **VAL-OS-ECO-004** — All three files exist under\n  `.github/ISSUE_TEMPLATE/` and are non-empty.\n\n# Commit guidance\n\nThis work commits to **os-eco main** (the meta-repo). The local\n`/Users/jayminwest/Projects/os-eco/.git` already has seven toolkit\ncommits ahead from earlier in this milestone — your commit lands on top\nof them on `main`.\n\nCommit message (single commit for this child):\n\n```\nos-eco: add governance scaffolding at root (closes VAL-OS-ECO-001..004)\n\n- AGENTS.md: ecosystem-wide guidance + sub-repo map\n- .github/dependabot.yml: github-actions weekly + cooldown\n- .github/pull_request_template.md: Summary / Changes / Test plan\n- .github/ISSUE_TEMPLATE/{bug_report,feature_request,config}.yml\n```\n\nDo **not** run `sd sync` or `git push` — the orchestrator handles\nseeds-state commits separately, and pushing is out of scope.\n\n# Pitfalls / off-limits\n\n- **Sub-repo directories are off-limits.** `warren/`, `burrow/`,\n  `plot/`, `mulch/`, `seeds/`, `canopy/`, `sapling/`, `overstory/` are\n  independent gits and explicitly listed in os-eco's `.gitignore`. A\n  root-scoped commit must NOT include any path inside those directories.\n- **No `npm` ecosystem in dependabot.yml.** The toolkit template\n  includes one for sub-repos; the os-eco ROOT has no package.json. If\n  dependabot.yml ships an `npm` entry pointing at `/`, Dependabot will\n  fail at runtime.\n- **Toolkit is in this repo, not warren/.** Read from\n  `templates/l5-toolkit/github/`, NOT `warren/.github/`. The toolkit is\n  the canonical source per VAL-CROSS-001.\n- **Do not modify `.github/workflows/auto-merge.yml`.** That file exists\n  at the os-eco root and is correct as-is; this child only adds files.\n- **Do not run `sd sync`, `git push`, or any seeds state mutations.**","createdAt":"2026-05-28T03:18:33.503Z","updatedAt":"2026-05-28T17:06:14.801Z","plan_id":"pl-ef44","blocks":["os-eco-f066"],"closedAt":"2026-05-28T17:06:14.801Z"}
{"id":"os-eco-9c17","title":"Extend root .gitignore and add labels.yml + sync-labels workflow","status":"closed","type":"task","priority":2,"plan_step_index":1,"description":"# Goal\n\nHarden the root `.gitignore` and add the canonical GitHub label taxonomy\nwith its sync workflow so ecosystem-level issues at the os-eco repo can\nbe labelled consistently. **Closes VAL-OS-ECO-005 and VAL-OS-ECO-006.**\n\nThis is the os-eco META repo (no package.json, no source, no tests). All\nfile paths are RELATIVE to the os-eco root. **Do not touch any sub-repo\ndirectory.** Depends on the previous child (`os-eco-352d`) having\nlanded; verify with `git log --oneline -3` before starting.\n\n# Working directory\n\n`/Users/jayminwest/Projects/os-eco/`\n\n`git status` should be clean (the previous commit landed on os-eco\nmain). Confirm before editing.\n\n# Reference docs\n\n- `docs/l5-uplift/library/criteria-mapping.md` — rows for\n  `gitignore_comprehensive` and `issue_labeling_system`.\n- `docs/l5-uplift/validation-contract.md` — VAL-OS-ECO-005 lists the\n  exact gitignore entries; VAL-OS-ECO-006 enumerates the artifacts.\n- `templates/l5-toolkit/github/labels.yml` — canonical label taxonomy.\n- `templates/l5-toolkit/github/workflows/sync-labels.yml` — workflow\n  template (warren's pattern: dry-run on PRs, full sync on push to main,\n  `skip-delete: true`).\n- Current state of `.gitignore`: contains only sub-repo dirs +\n  `.run-plan-logs/` + `.DS_Store`. Preserve those lines verbatim.\n\n# Files to edit / add\n\n1. **Edit `.gitignore` (at root)** — Append (do NOT replace) the\n   comprehensive baseline so it matches what `gitignore_comprehensive`\n   expects per `criteria-mapping.md`. The merged file must include\n   ALL of:\n   - The existing sub-repo entries (warren/, burrow/, plot/, mulch/,\n     seeds/, canopy/, sapling/, overstory/, and the now-archived\n     greenhouse/). Do not delete any sub-repo line.\n   - The existing `.run-plan-logs/` line.\n   - The existing `.DS_Store` line (or absorb it into the OS-file\n     section).\n   - `.env`\n   - `.env.*` followed by `!.env.example` (negate-pattern so the example\n     stays tracked even though there isn't one today — future-proofs\n     the meta-repo for env-var docs).\n   - `.idea/`\n   - `.vscode/`\n   - `*.log`\n   - `node_modules/` (cheap insurance even though no package.json today)\n   - `dist/` (same rationale)\n   - `coverage/` (same)\n   - `test-results/` (same)\n\n   Suggested final structure (grouped with comments for readability):\n\n   ```gitignore\n   # Sub-repos (independently managed)\n   burrow/\n   canopy/\n   greenhouse/\n   mulch/\n   overstory/\n   plot/\n   sapling/\n   seeds/\n   warren/\n\n   # Local logs / scratch\n   .run-plan-logs/\n\n   # Secrets / env\n   .env\n   .env.*\n   !.env.example\n\n   # Editor / IDE\n   .idea/\n   .vscode/\n\n   # OS junk\n   .DS_Store\n\n   # Build / test artifacts (defensive — no build at root today)\n   node_modules/\n   dist/\n   coverage/\n   test-results/\n   *.log\n   ```\n\n2. **Add `.github/labels.yml` (new)** — Copy verbatim from\n   `templates/l5-toolkit/github/labels.yml`. It defines:\n   - `priority/P0`..`priority/P3` (red ramp).\n   - `kind/{bug,feature,chore}` (blue ramp).\n   - `area/{cli,docs,test,ci,deps}` (green ramp).\n   - `status/{needs-triage,blocked,in-progress,needs-review,needs-info,\n     wontfix,duplicate}` (yellow ramp).\n\n   The ROOT meta-repo's \"area\" labels are intentionally generic — the\n   per-sub-repo `area/cli` etc. still apply because issues filed against\n   the meta-repo usually point at a sub-repo. Do NOT add ecosystem-\n   specific labels (`area/branding`, `area/ecosystem`) in this child;\n   the toolkit taxonomy is the canonical source of truth for\n   VAL-CROSS-001 consistency. If they're needed later, file a follow-up\n   seed.\n\n3. **Add `.github/workflows/sync-labels.yml` (new)** — Copy verbatim\n   from `templates/l5-toolkit/github/workflows/sync-labels.yml`. The\n   workflow:\n   - Triggers on push to main when `labels.yml` or `sync-labels.yml`\n     changes.\n   - Triggers on PR for dry-run review.\n   - Has `workflow_dispatch` with a `dry-run` input.\n   - Uses `crazy-max/ghaction-github-labeler@v5` with\n     `skip-delete: true` (additive, preserves bot-created labels).\n\n# Verification\n\n```bash\ncd /Users/jayminwest/Projects/os-eco/\n# .gitignore sanity\ngrep -E \"^(\\.env|\\.env\\.\\*|\\.idea/|\\.vscode/|\\*\\.log)\" .gitignore\ngrep -E \"^(burrow|canopy|warren|seeds|mulch|plot|sapling|overstory)/\" .gitignore\n# Files exist\nls .github/labels.yml .github/workflows/sync-labels.yml\n# YAML validity\npython3 -c \"import yaml; yaml.safe_load(open('.github/labels.yml'))\"\npython3 -c \"import yaml; yaml.safe_load(open('.github/workflows/sync-labels.yml'))\"\n# Sub-repos still ignored (very important)\ngit check-ignore -v burrow/ canopy/ warren/ seeds/ mulch/ plot/ \\\n                    sapling/ overstory/\n# git status shows only the two new files + modified .gitignore\ngit status\n```\n\n# Acceptance criteria closed\n\n- **VAL-OS-ECO-005** — `.gitignore` contains `.env`, `.env.*`, `.idea/`,\n  `.vscode/`, `.DS_Store`, `*.log` in addition to the existing sub-repo\n  ignores.\n- **VAL-OS-ECO-006** — Both `.github/labels.yml` (P0–P3 + area labels)\n  and `.github/workflows/sync-labels.yml` exist; the workflow triggers\n  on push to main when labels.yml changes.\n\n# Commit guidance\n\nCommit to **os-eco main**:\n\n```\nos-eco: harden .gitignore + add label taxonomy (closes VAL-OS-ECO-005,006)\n\n- .gitignore: add .env, .env.*, IDE dirs, OS files, build artifacts\n- .github/labels.yml: canonical P0–P3 / kind / area / status taxonomy\n- .github/workflows/sync-labels.yml: warren's mirror pattern,\n  skip-delete=true, dry-run on PRs\n```\n\n# Pitfalls / off-limits\n\n- **Sub-repo directories are off-limits.** Never `git add` anything\n  inside `warren/`, `burrow/`, `plot/`, `mulch/`, `seeds/`, `canopy/`,\n  `sapling/`, `overstory/`, or `greenhouse/`. They are independently\n  versioned and intentionally gitignored from the root.\n- **Do NOT remove the sub-repo lines from .gitignore.** Losing them\n  would cause the next root commit to recursively track every\n  sub-repo's working tree — a massive accidental commit. Append, never\n  replace.\n- **Toolkit is the canonical source.** Copy from\n  `templates/l5-toolkit/github/labels.yml` and\n  `templates/l5-toolkit/github/workflows/sync-labels.yml`, NOT from\n  `warren/.github/`. VAL-CROSS-001 requires diff-equivalence between\n  toolkit and consumers.\n- **`actions/checkout@v6` is the warren-validated pinned version.** If\n  the toolkit template uses a different version, prefer the toolkit\n  version verbatim.\n- **Do not run `sd sync` or `git push`.** Orchestrator handles\n  seeds-state commits; pushing is out of scope.","createdAt":"2026-05-28T03:18:33.503Z","updatedAt":"2026-05-28T16:59:12.056Z","plan_id":"pl-ef44","blocks":["os-eco-f066"],"closedAt":"2026-05-28T16:59:12.056Z"}
{"id":"os-eco-d8f0","title":"Make L5 toolkit discoverable from root docs","status":"closed","type":"task","priority":2,"plan_step_index":2,"description":"# Goal\n\nMake the L5 toolkit (`templates/l5-toolkit/`) discoverable from the\ntop-level docs at the os-eco root. A new contributor opening\n`README.md` or `CLAUDE.md` must be able to find the toolkit and\nunderstand its purpose without external context. **Closes\nVAL-OS-ECO-007.**\n\nThis is the os-eco META repo. All file paths are RELATIVE to the os-eco\nroot. **Do not touch any sub-repo directory.** Depends on the previous\nchild (`os-eco-9c17`) having landed; verify with\n`git log --oneline -3`.\n\n# Working directory\n\n`/Users/jayminwest/Projects/os-eco/`\n\n`git status` should be clean. Confirm before editing.\n\n# Reference docs\n\n- `docs/l5-uplift/validation-contract.md` — VAL-OS-ECO-007 spells out\n  the evidence sentence: \"either `README.md` or `CLAUDE.md` contains a\n  paragraph or link pointing at `templates/l5-toolkit/`\".\n- `templates/l5-toolkit/README.md` — the toolkit's own ~26 KB\n  documentation. Read the first ~100 lines so the section you write\n  accurately summarizes what's in the toolkit.\n- Source feature description (the parent seed body, or the upstream\n  reference): names the toolkit components to mention: scripts,\n  configs, docs, env, skills, package-scripts.\n\n# Files to edit\n\n**Pick ONE primary surface, then optionally cross-link from the other.**\nThe mission contract requires the toolkit to be referenced from at\nleast one of `README.md` or `CLAUDE.md`. Adding it to BOTH is fine and\nslightly preferable for discoverability, but not required.\n\nRecommended: add the toolkit section to `CLAUDE.md` (it already has\nextensive ecosystem context — agents read it first). Optionally add a\none-line pointer near the end of `README.md` too.\n\n## Required: edit `CLAUDE.md`\n\nAdd a new top-level section titled **\"L5 Toolkit\"** (or similar). Place\nit after the existing \"Scripts\" section and before \"Conventions\". The\nsection MUST:\n\n1. Name the directory: `templates/l5-toolkit/`.\n2. Explain its purpose in one paragraph: the canonical source of\n   portable L5-readiness artifacts (ratchet scripts, biome/knip/jscpd\n   configs, devcontainer, env example, skills, package-scripts), copied\n   into each sub-repo during the L5 uplift mission.\n3. List what's in it at a high level:\n   - `scripts/` — ratchet scripts (file-size, debt-markers, coverage),\n     reporters (test-timing, quality-metrics), validator\n     (validate-agents-md), pre-commit hook.\n   - `configs/` — biome.json, knip.json, .jscpd.json, bunfig.toml,\n     tsconfig.base.json.\n   - `budgets/` — baseline JSON budgets for ratchet scripts.\n   - `docs/` — documentation templates.\n   - `env/` — `.env.example` template.\n   - `skills/` — `.factory/skills/<name>/SKILL.md` template.\n   - `github/` — dependabot.yml, labels.yml, sync-labels workflow,\n     issue templates, PR template (also consumed by this root repo's\n     governance scaffolding).\n   - `package-scripts.json` — canonical `package.json` script entries.\n4. Link to `templates/l5-toolkit/README.md` for the full per-artifact\n   adoption procedure.\n5. Note that the toolkit is **the source of truth** for VAL-CROSS-001\n   consistency: each sub-repo's ported artifact must be diff-equivalent\n   modulo budgets/tracker prefixes/paths.\n\nUse the existing markdown idioms in `CLAUDE.md` (table-of-tools style,\n`<!-- … -->` HTML comments for tool-managed sections are NOT\nappropriate here — this is hand-authored content).\n\n## Optional: edit `README.md`\n\nIf you also touch `README.md`, add a single short paragraph near the\n\"Repo structure\" section (or at the very end) pointing readers at the\ntoolkit:\n\n> **L5 Toolkit.** The `templates/l5-toolkit/` directory holds the\n> portable L5-readiness artifacts (ratchet scripts, configs, env, CI\n> templates, skills) used to uplift each sub-repo. See\n> [`templates/l5-toolkit/README.md`](templates/l5-toolkit/README.md).\n\n# Verification\n\n```bash\ncd /Users/jayminwest/Projects/os-eco/\n# Required: at least one of these greps non-zero\ngrep -F \"templates/l5-toolkit\" CLAUDE.md\ngrep -F \"templates/l5-toolkit\" README.md || true\n# Section heading detection\ngrep -E \"^#+ +L5 [Tt]oolkit\" CLAUDE.md\ngit diff --stat\ngit status\n```\n\nThe diff should show edits ONLY to `CLAUDE.md` (and optionally\n`README.md`). Nothing else should be modified.\n\n# Acceptance criteria closed\n\n- **VAL-OS-ECO-007** — At least one of `README.md` or `CLAUDE.md`\n  contains a section pointing at `templates/l5-toolkit/` with a link to\n  its README.\n\n# Commit guidance\n\nCommit to **os-eco main**:\n\n```\nos-eco: surface L5 toolkit in root docs (closes VAL-OS-ECO-007)\n\nAdd an \"L5 Toolkit\" section to CLAUDE.md summarizing the contents of\ntemplates/l5-toolkit/ and linking to its README. [Optional: also add a\nshort pointer in README.md's Repo structure section.]\n```\n\n# Pitfalls / off-limits\n\n- **Don't restructure unrelated parts of `CLAUDE.md` or `README.md`.**\n  Both files already have substantial existing content; insert a single\n  new section, don't refactor adjacent sections.\n- **Sub-repo directories are off-limits.** No edits inside `warren/`,\n  `burrow/`, `plot/`, `mulch/`, `seeds/`, `canopy/`, `sapling/`,\n  `overstory/`.\n- **Link target uses repo-relative paths.** Write\n  `templates/l5-toolkit/README.md`, not absolute paths or GitHub URLs —\n  the validation contract checks for the string\n  `templates/l5-toolkit`.\n- **Auto-managed sections.** `CLAUDE.md` contains\n  `<!-- mulch:start -->`, `<!-- seeds:start -->`, and\n  `<!-- canopy:start -->` blocks managed by their respective onboard\n  commands. Do NOT add the L5 Toolkit section inside any of those\n  blocks — `ml prime` / `sd prime` / `cn prime` would overwrite it.\n  Place it above the mulch block (around the existing \"Scripts\" /\n  \"Conventions\" section).\n- **Do not run `sd sync` or `git push`.**","createdAt":"2026-05-28T03:18:33.503Z","updatedAt":"2026-05-28T17:00:39.485Z","plan_id":"pl-ef44","blocks":["os-eco-f066"],"closedAt":"2026-05-28T17:00:39.485Z"}
{"id":"os-eco-3ce3","title":"Finalize for audit: verify governance, request /readiness-report","status":"in_progress","type":"task","priority":2,"plan_step_index":3,"description":"# Goal\n\nSeal the os-eco META milestone — the FINAL milestone of the entire L5\nuplift mission across the ecosystem. Verify that all governance files\nfrom the three preceding children are present and committed, that the\nworking tree is clean, and that the handoff to the user requests a\n`/readiness-report` against the os-eco root so the orchestrator can\nmark VAL-OS-ECO-FINAL and the cross-area assertions VAL-CROSS-001..006.\n\n**Closes VAL-OS-ECO-FINAL, VAL-CROSS-001, VAL-CROSS-002, VAL-CROSS-003,\nVAL-CROSS-004, VAL-CROSS-005, VAL-CROSS-006.**\n\n(The VAL-CROSS-* assertions also depend on per-sub-repo audits already\nbeing in place from milestones 1–5; this child confirms the os-eco\npiece of those invariants. The orchestrator evaluates the cross-area\nassertions after consuming the user's pasted `/readiness-report`.)\n\nThis child does **no new feature work** — it is a verification + handoff\nseal. Depends on children `os-eco-352d`, `os-eco-9c17`, `os-eco-d8f0`\nhaving landed.\n\n# Working directory\n\n`/Users/jayminwest/Projects/os-eco/`\n\n# Reference docs\n\n- `docs/l5-uplift/library/porting-playbook.md` — the \"Milestone Seal\n  Procedure\" section spells out the seal steps. Adapt to the meta-repo\n  (no `bun run check:all` here; the meta-repo has no Bun project).\n- `docs/l5-uplift/validation-contract.md` — VAL-OS-ECO-FINAL +\n  VAL-CROSS-001..006 evidence sentences.\n- `docs/l5-uplift/MISSION-AGENTS.md` — confirms this is the LAST\n  milestone, the wrap-up of the entire mission.\n\n# Verification (this child IS the verification step)\n\nRun these checks from `/Users/jayminwest/Projects/os-eco/`. Each must\npass before composing the handoff.\n\n```bash\n# Governance files from child 1 (VAL-OS-ECO-001..004)\ntest -s AGENTS.md\ntest \"$(wc -c < AGENTS.md)\" -gt 2048\ngrep -qi \"ecosystem\" AGENTS.md\ntest -s .github/dependabot.yml\ngrep -q \"github-actions\" .github/dependabot.yml\ntest -s .github/pull_request_template.md\ngrep -qi \"Summary\" .github/pull_request_template.md\ngrep -qi \"Changes\" .github/pull_request_template.md\ngrep -qi \"Test plan\" .github/pull_request_template.md\ntest -s .github/ISSUE_TEMPLATE/bug_report.yml\ntest -s .github/ISSUE_TEMPLATE/feature_request.yml\ntest -s .github/ISSUE_TEMPLATE/config.yml\n\n# .gitignore + labels from child 2 (VAL-OS-ECO-005..006)\ngrep -qE \"^\\.env$\" .gitignore\ngrep -qE \"^\\.env\\.\\*\" .gitignore\ngrep -qE \"^\\.idea/$\" .gitignore\ngrep -qE \"^\\.vscode/$\" .gitignore\ngrep -qE \"^\\.DS_Store$\" .gitignore\ngrep -qE \"^\\*\\.log$\" .gitignore\ngrep -qE \"^burrow/$\" .gitignore     # sub-repo ignores preserved\ngrep -qE \"^warren/$\" .gitignore\ntest -s .github/labels.yml\ngrep -q \"priority/P0\" .github/labels.yml\ntest -s .github/workflows/sync-labels.yml\n\n# Toolkit discoverability from child 3 (VAL-OS-ECO-007)\ngrep -qF \"templates/l5-toolkit\" CLAUDE.md || \\\n  grep -qF \"templates/l5-toolkit\" README.md\n\n# Clean tree, all governance tracked\ngit status --porcelain | grep -v \"^?? \" | head -1   # no unstaged/staged drift\ngit ls-files AGENTS.md .github/dependabot.yml \\\n             .github/pull_request_template.md \\\n             .github/ISSUE_TEMPLATE/bug_report.yml \\\n             .github/ISSUE_TEMPLATE/feature_request.yml \\\n             .github/ISSUE_TEMPLATE/config.yml \\\n             .github/labels.yml \\\n             .github/workflows/sync-labels.yml\ngit log --oneline -8        # should show the three child commits\n```\n\nThere is no `bun run check:all`, no `bun test`, no `bun run lint` at\nthis level — the meta-repo has no package.json. **That is intentional**:\nthe auditor classifies os-eco as having 0 applications and skips\napplication-scope criteria.\n\nUntracked files that EXISTED before the mission (e.g.,\n`.factory-mission-scratch/`, `ai-readiness-principles.md`,\n`branding/concepts/`, `docs/l5-uplift/`,\n`readiness-report-prompt.md`, `scripts/set-npm-token.sh`) are allowed\nto remain untracked — they are user-owned and outside the scope of\nthis milestone. The `git status` check above filters `^?? ` to allow\nthem.\n\n# Acceptance criteria closed\n\n- **VAL-OS-ECO-FINAL** — os-eco audit reports Level 5; application-\n  scope criteria appear as numerator=null (skipped); repo-scope\n  criteria covered by VAL-OS-ECO-001..007 are at 1/1. Verified by the\n  user's `/readiness-report` output (this child only requests the run;\n  the orchestrator records the result).\n- **VAL-CROSS-001** — Toolkit consistency. Re-verify by spot-checking\n  that the artifacts copied into the os-eco root (dependabot.yml,\n  pr template, issue templates, labels.yml, sync-labels.yml) are\n  byte-equivalent to or trivially adapted from `templates/l5-toolkit/\n  github/`. Note any intentional adaptations in the handoff (e.g.,\n  dependabot.yml drops the npm ecosystem because the root has no\n  package.json — that's an expected delta, NOT a logic divergence).\n- **VAL-CROSS-002..005** — Cross-repo invariants; this child only seals\n  the os-eco contribution. The orchestrator evaluates these against\n  the sub-repos' final audits already in `.factory-mission-scratch/`.\n- **VAL-CROSS-006** — Audit paste-back trail. The handoff request for\n  `/readiness-report` produces the os-eco entry in this trail.\n\n# Handoff / commit guidance\n\nThis child likely produces **zero file changes** — it is a verification\nstep. If you find a missing file or a broken evidence assertion, fix it\nin this child (commit with a message naming the criterion that needed\nthe fix). Otherwise:\n\n1. Compose a `salientSummary` that includes a clearly-marked line:\n\n   > **Ready for audit.** Please run `/readiness-report` from\n   > `/Users/jayminwest/Projects/os-eco/` (no `--repo` flag — this is\n   > the meta-repo root). Paste the output back into this session so\n   > the orchestrator can mark VAL-OS-ECO-FINAL and the VAL-CROSS-*\n   > assertions.\n\n2. List in `whatWasImplemented`:\n   - \"Sealed os-eco meta-repo: VAL-OS-ECO-001..007 verified.\"\n   - \"Requested final `/readiness-report` for VAL-OS-ECO-FINAL +\n     VAL-CROSS-001..006.\"\n\n3. Do NOT push or run `sd sync`. The orchestrator handles seeds-state\n   commits after the audit lands.\n\n4. If you DID need to commit a fix during verification, the message\n   format is:\n\n   ```\n   os-eco: seal milestone — <fix description> (closes VAL-OS-ECO-FINAL)\n   ```\n\n# Pitfalls / off-limits\n\n- **Sub-repo directories are off-limits.** Even during verification, do\n  not `cd` into sub-repos or run their test suites — those audits are\n  already done in milestones 1–5 and recorded in\n  `.factory-mission-scratch/<repo>-readiness-report.md`. This child\n  ONLY verifies the os-eco root.\n- **No `bun` commands at the root.** There is no package.json. Don't\n  fabricate a `check:all` here.\n- **Do not modify `.factory-mission-scratch/` or anything under\n  `docs/l5-uplift/`.** Those are mission-state and reference docs;\n  workers don't write there. The orchestrator updates\n  `validation-state.json` after the user pastes the report.\n- **Do not run `sd sync` or `git push`.**\n- **No additional dependabot ecosystems.** Resist the urge to add an\n  `npm` ecosystem to dependabot.yml \"just to be safe\" — the meta-repo\n  legitimately has no package.json and the github-actions ecosystem\n  alone is the correct configuration per VAL-OS-ECO-002.\n- **Audit expectation:** the auditor will likely classify os-eco as\n  having 0 \"apps\" (no `bun.lockb`, no `package.json`, no entry-point\n  pattern) and SKIP most application-scope criteria. That is the\n  expected and correct behaviour — do NOT try to create a fake\n  package.json to \"fix\" the skip count.","createdAt":"2026-05-28T03:18:33.503Z","updatedAt":"2026-05-28T17:01:15.631Z","plan_id":"pl-ef44","blocks":["os-eco-f066"]}
{"id":"os-eco-a445","title":"Uplift all os-eco sub-repos to kota-monorepo standards (frozen gate manifest, parity, boundaries, secrets, golden contracts, observability, runtime smoke)","status":"open","type":"epic","priority":1,"createdAt":"2026-06-11T04:11:59.597Z","updatedAt":"2026-06-11T04:11:59.597Z","description":"A 14-repo scout sweep (2026-06-10) found the kota fleet has evolved past the os-eco L5 toolkit. Bring warren, burrow, plot, mulch, seeds, canopy, sapling, and trellis up to kota-monorepo standard (reference: kota-monorepo docs/check-all-standard.md + docs/observability-standard.md, with kota-memory/kota-sense as the most complete implementations). Per repo: (1) frozen ordered GATES manifest — scripts/check-all.ts with exported GATES array as the single source of truth, quiet-runner output contract; (2) check:ci-parity as the final gate — CI may invoke nothing outside the manifest (kills CI drift structurally); (3) check:boundaries — dependency-cruiser layering as hard errors encoding each repo's documented architecture (e.g. trellis api>cli>sdk, plot store/cli seams); (4) check:secrets — gitleaks working-tree scan locally + history-aware in CI; (5) golden contract locks where a contract exists (warren OpenAPI already has gen:openapi:check — promote to byte-diff golden; plot/seeds/mulch JSON output shapes); (6) 7-rule observability/silent-failure contract (no bare catch, correlation ids, worker /status, shell-script contract: set -euo pipefail, port-poll before success, append-only logs); (7) per-incident runbooks beyond release (daemon-down/store-corruption/provider-outage class playbooks); (8) runtime smoke wired into check:all / a verify alias — boot the artifact (CLI --version round-trip, daemon /health) so 'it actually runs' is part of the local gate, executable in burrow within warren too; (9) verify alias = exactly the full gate, no lean subsets. Trellis canonical-standards v2 (trellis-79c9) encodes these as the bundled standards/ + rubric criteria; this epic is the fleet-side adoption. Suggested decomposition: one child per sub-repo plus one for the shared template tree (templates/l5-toolkit -> v2)."}
{"id":"os-eco-3ee9","title":"Adopt canonical check:all standard across the os-eco fleet","status":"open","type":"feature","priority":1,"createdAt":"2026-06-12T04:50:05.880Z","updatedAt":"2026-06-12T06:03:17.071Z","plan_id":"pl-760e","description":"Fleet rollout of the canonical check:all standard (quiet runner + verify alias + ci-parity), modeled on kota-monorepo's docs/check-all-standard.md. This root tracker owns the cross-repo foundation via plan pl-760e: (1) os-eco-9048 author docs/check-all-standard.md, (2) os-eco-5db7 build the portable runner + ci-parity into templates/l5-toolkit, (3) os-eco-9a3a fleet close-out/parity verification.\n\nPer-repo conformance plans (each consumes the foundation; sd cannot express cross-store deps, so each repo plan should land only AFTER os-eco-9048 + os-eco-5db7):\n- warren   pl-cf2a (warren-16d2)  -- rename verbose gates to canonical + adopt runner + refactor existing ci-parity\n- burrow   pl-beec (burrow-8d25)  -- GREENFIELD: port l5-toolkit scripts/configs + runner + ci-parity\n- sapling  pl-4a1c (sapling-a83c) -- GREENFIELD: port l5-toolkit scripts/configs + runner + ci-parity\n- plot     pl-b50c (plot-9be7)    -- adopt runner (+add lint/typecheck to manifest) + ci-parity + verify\n- mulch    pl-237d (mulch-f16f)   -- adopt runner (+add lint/typecheck to manifest) + ci-parity + verify\n- seeds    pl-78eb (seeds-67a6)   -- adopt runner (+add lint/typecheck to manifest) + ci-parity + verify\n- trellis  pl-134e (trellis-7ffe) -- adopt runner (reorder coverage) + ci-parity + verify\n- canopy   pl-a59c (canopy-9e11)  -- adopt runner (+fold in check:coverage + gen:docs:check) + ci-parity + verify\n\nos-eco-9a3a (close-out) gates on all eight per-repo plans completing."}
{"id":"os-eco-9048","title":"Author docs/check-all-standard.md at the os-eco root: freeze the canonical terse gate vocabulary (check:size/debt/dups/deps/agents/coverage + lint/typecheck), the ordered core gate list (lint, typecheck, check:agents, check:dups, check:deps, check:size, check:debt, [conditional gates], check:coverage, check:ci-parity LAST), a conditional-gate registry (check:bundle-size for bundle producers, gen:docs:check for doc generators, gen:openapi:check for OpenAPI servers), the quiet-output contract (one aligned `<status> <gate> (N.Ns)` line per gate; on failure only failing gate names + parsed test signatures; CHECK_ALL_VERBOSE/--bail escape hatches), the `verify` = `bun run check:all` rule, and the check:ci-parity rule. Include a per-repo reconciliation table mapping each repo's current names to canonical (esp. warren's verbose keys). Doc-only; changes no repo behavior.","status":"closed","type":"task","priority":1,"plan_step_index":0,"description":"<!-- seeds:plan-backref:start -->\nStep 1 of plan pl-760e.\n\nParent seed: os-eco-3ee9 — Adopt canonical check:all standard across the os-eco fleet\nPlan template: feature\nPlan approach: Land the cross-repo foundation here, then let each repo conform via its own sd plan (warren, burrow, plot, mulch, seeds, canopy, sapling, trellis). Step 1 writes docs/check-all-standard.md freezing the canonical terse vocabulary (matching…\n\nRun `sd plan show pl-760e` for the full plan (context, alternatives, sibling steps, acceptance criteria).\n<!-- seeds:plan-backref:end -->","createdAt":"2026-06-12T04:50:50.648Z","updatedAt":"2026-06-12T06:03:16.973Z","plan_id":"pl-760e","blocks":["os-eco-5db7","os-eco-3ee9"],"closedAt":"2026-06-12T06:03:16.973Z"}
{"id":"os-eco-5db7","title":"Build the portable quiet runner + generalized ci-parity in templates/l5-toolkit/scripts: add check-all.ts (exports a GATES array run as data, captures each gate's output, prints the aligned per-gate lines + tally on success and signatures-only on failure, honors CHECK_ALL_VERBOSE and --bail) and check-ci-parity.ts (imports GATES from check-all.ts, parses .github/workflows/ci*.yml, fails on any `bun run <x>` not reachable from check:all, with ALIASES/CI_ONLY escape hatches) plus co-located *.test.ts; port warren/scripts/check-ci-parity.ts as the reference implementation. Update templates/l5-toolkit/package-scripts.json so check:all becomes `bun scripts/check-all.ts`, add `verify`: `bun run check:all` and `check:ci-parity`: `bun scripts/check-ci-parity.ts`, add the `yaml` devDep. Document the runner + verify + ci-parity wiring in templates/l5-toolkit/README.md.","status":"closed","type":"task","priority":1,"plan_step_index":1,"description":"<!-- seeds:plan-backref:start -->\nStep 2 of plan pl-760e.\n\nParent seed: os-eco-3ee9 — Adopt canonical check:all standard across the os-eco fleet\nPlan template: feature\nPlan approach: Land the cross-repo foundation here, then let each repo conform via its own sd plan (warren, burrow, plot, mulch, seeds, canopy, sapling, trellis). Step 1 writes docs/check-all-standard.md freezing the canonical terse vocabulary (matching…\n\nRun `sd plan show pl-760e` for the full plan (context, alternatives, sibling steps, acceptance criteria).\n<!-- seeds:plan-backref:end -->","createdAt":"2026-06-12T04:50:50.648Z","updatedAt":"2026-06-12T06:03:17.023Z","plan_id":"pl-760e","blocks":["os-eco-9a3a","os-eco-3ee9"],"closedAt":"2026-06-12T06:03:17.023Z"}
{"id":"os-eco-9a3a","title":"Fleet close-out and parity verification: after the eight per-repo conformance plans land, run `bun run check:all` and `bun run verify` in every sub-repo and confirm each uses the byte-identical scripts/check-all.ts runner, the canonical gate names in the standard's order, a green check:ci-parity, and the quiet-output contract (one line per gate, signatures-only on failure). Update the root CLAUDE.md 'Build & Test Commands' section to standardize on `bun run check:all` / `bun run verify`, and record an ecosystem-domain mulch decision capturing the adopted standard.","status":"closed","type":"task","priority":2,"plan_step_index":2,"description":"<!-- seeds:plan-backref:start -->\nStep 3 of plan pl-760e.\n\nParent seed: os-eco-3ee9 — Adopt canonical check:all standard across the os-eco fleet\nPlan template: feature\nPlan approach: Land the cross-repo foundation here, then let each repo conform via its own sd plan (warren, burrow, plot, mulch, seeds, canopy, sapling, trellis). Step 1 writes docs/check-all-standard.md freezing the canonical terse vocabulary (matching…\n\nRun `sd plan show pl-760e` for the full plan (context, alternatives, sibling steps, acceptance criteria).\n<!-- seeds:plan-backref:end -->","createdAt":"2026-06-12T04:50:50.648Z","updatedAt":"2026-06-12T06:03:17.071Z","plan_id":"pl-760e","blocks":["os-eco-3ee9"],"closedAt":"2026-06-12T06:03:17.071Z"}
{"id":"os-eco-49b3","title":"check:all standard rev 2: reformat frozen runner scripts to be biome-format-clean, drop per-repo formatter overrides","status":"open","type":"task","priority":3,"createdAt":"2026-06-12T06:03:09.920Z","updatedAt":"2026-06-12T06:03:09.920Z","description":"Five repo agents independently found that templates/l5-toolkit/scripts/check-all.ts and check-ci-parity.ts are not format-clean under the canonical biome config (lineWidth 100, biome 2.4.14+ wants to reformat check-all.ts ~l.144 and check-ci-parity.ts ~l.108). Rev 1 ships a formatter-off override scoped to the two frozen files (canonical in templates/l5-toolkit/configs/biome.json, mirrored in every repo). A future standard rev should reformat the template scripts, re-sync all eight repos (byte-identity means a coordinated fleet update), and drop the overrides."}
